export enum Maps {
  Map = 0,
}

export const MapKeys: string[] = ['map'];

export enum Textures {
  Tiles = 0,
  Player = 1,
  Slime = 2,
}

export const TextureKeys: string[] = ['tiles', 'player', 'slime'];
